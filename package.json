{
  "name": "adriansieber",
  "private": true,
  "scripts": {
    "start": "stylus --watch --out ./css styl & jekyll serve --trace --drafts",
    "build": "npm run buildCss && npm run buildResume && jekyll build",
    "buildResume": "node ./_resume/index.js",
    "buildCss": "stylus --compress --out ./css styl"
  },
  "dependencies": {
    "handlebars": "^2.0.0-alpha.4",
    "js-yaml": "^3.2.6"
  }
}
